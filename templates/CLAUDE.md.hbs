# Lore — Agent Context ## Project Identity - **Name:**
{{project.name}}
- **Slug:**
{{project.slug}}
- **Lore Server:**
{{mcp.server}}
- **Lore MCP Server Name:** `lore-memory-{{project.slug}}` ## Repositories

{{#each repos}}
  ###
  {{name}}
  (`{{slug}}`) - **Path:** `{{path}}` - **Stack:**
  {{#each stack}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}

{{/each}}
## Agent Standards - All code must follow the project's established conventions. -
{{agentStandards.styleRule}}
- Before every commit:
{{agentStandards.commitChecks}}.
{{#if methodology}}

  ## Methodology - **Type:**
  {{methodology.type}}
  - **Version:**
  {{methodology.version}}
  {{#if methodology.default_dev_skill}}- **Default Dev Skill:**
    {{methodology.default_dev_skill}}
  {{/if}}{{#if methodology.default_review_skill}}- **Default Review Skill:**
    {{methodology.default_review_skill}}
  {{/if}}{{/if}}